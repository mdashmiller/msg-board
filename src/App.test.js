import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('passes automagically', () => {
	expect(1).toBe(1)
  // const div = document.createElement('div');
  // ReactDOM.render(<App />, div);
  // ReactDOM.unmountComponentAtNode(div);
});
