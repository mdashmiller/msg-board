// an array of keys that will still work
// when the char limit has been
// reached on a form field
const Keys = {
	list: ['Backspace', 'ArrowLeft', 'ArrowRight', 'Enter', 'Tab', 'Home', 'End']
}

export default Keys
